{
  "name": "@patchrelay/openclaw-plugin",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "OpenClaw tool plugin shim for PatchRelay.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "plugin:build": "npm run build && openclaw plugins build --root . --entry ./dist/index.js",
    "plugin:validate": "npm run build && openclaw plugins validate --root . --entry ./dist/index.js",
    "test": "node --test --import tsx src/*.test.ts"
  },
  "files": [
    "dist",
    "openclaw.plugin.json",
    "README.md"
  ],
  "peerDependencies": {
    "openclaw": ">=2026.6.1"
  },
  "dependencies": {
    "typebox": "^1.2.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "openclaw": "^2026.6.1",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "engines": {
    "node": ">=22"
  }
}
